# CCF — Canadian Climate Framing
# Python dependencies of the deposited pipeline (Python 3.11+).
#
# Version floors, not exact pins: the pipeline is not sensitive to patch
# releases, and floors keep the environment installable on current Python.
# See REPRODUCING.md for the step-by-step reproduction guide.

# --- Core data handling -----------------------------------------------------
pandas>=2.0
numpy>=1.24
scipy>=1.10

# --- PostgreSQL access ------------------------------------------------------
psycopg2-binary>=2.9

# --- Machine learning: training (06) and annotation (07) --------------------
torch>=2.0
transformers>=4.30
scikit-learn>=1.3
AugmentedSocialScientist>=0.1.0   # see note below

# --- NLP preprocessing (01) and named-entity recognition (08) --------------
spacy>=3.7

# --- Inter-coder reliability (13) ------------------------------------------
krippendorff>=0.5.1

# --- Figures (14, and the Results plotting scripts) ------------------------
matplotlib>=3.7
seaborn>=0.13

# --- Parquet export (Scripts/Database_creation/enrichment/05) --------------
polars>=0.20
duckdb>=0.10

# --- Utilities --------------------------------------------------------------
tqdm>=4.65
joblib>=1.3
openpyxl>=3.1

# Notes
# -----
# * AugmentedSocialScientist: the pipeline uses a custom fork that adds
#   per-epoch metric logging, weighted best-model selection, and the
#   reinforced-training protocol. Install it from
#   https://github.com/antoinelemor/AugmentedSocialScientist_fork
#   rather than from PyPI if you need to reproduce training exactly.
# * spaCy also needs the French model `fr_core_news_lg`, pulled on first use:
#       python -m spacy download fr_core_news_lg
# * The transformer checkpoints (bert-base-cased, camembert-base, the two NER
#   models, and BAAI/bge-m3) are downloaded from the Hugging Face Hub on first
#   use; they are not vendored here.
# * geopandas is required only by the optional provincial map in
#   Results/Scripts/1_overview_plots.py, not by the annotation pipeline.
